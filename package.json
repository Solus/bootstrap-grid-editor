{
  "name": "bootstrap-visualizer",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Solus/bootstrap-grid-editor"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @bootstrap-visualizer/app-standalone",
    "build": "tsc -b",
    "build:core": "tsc -b packages/core --force",
    "build:single": "npm run build:single -w @bootstrap-visualizer/app-standalone",
    "pretest": "npm run build:core",
    "test": "vitest run",
    "pretest:watch": "npm run build:core",
    "test:watch": "vitest",
    "pretest:e2e": "npm run build:core",
    "test:e2e": "npm run test:e2e -w @bootstrap-visualizer/app-standalone",
    "test:vscode": "npm run test:vscode -w bootstrap-grid-editor",
    "test:all": "npm run test && npm run test:e2e && npm run test:vscode",
    "typecheck": "tsc -b && tsc -p packages/core/tsconfig.spec.json && tsc -p packages/editor/tsconfig.json && tsc -p packages/app-standalone/tsconfig.json && tsc -p packages/app-standalone/tsconfig.e2e.json && tsc -p packages/app-extension/tsconfig.host.json && tsc -p packages/app-extension/tsconfig.webview.json && tsc -p packages/app-extension/tsconfig.integration.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
