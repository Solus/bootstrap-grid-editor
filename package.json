{
  "name": "bootstrap-visualizer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run test:e2e -w @bootstrap-visualizer/app-standalone",
    "test:all": "npm run test && npm run test:e2e",
    "typecheck": "tsc -b && tsc -p packages/core/tsconfig.spec.json && tsc -p packages/app-standalone/tsconfig.json && tsc -p packages/app-standalone/tsconfig.e2e.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
