node_modules/
dist/
dist-single/
out/
*.vsix
.env
.env.*
!.env.example
.vscode-test/
# Keep the shared launch/tasks configs tracked, but not personal/dev-written
# workspace settings (the extension writes its own conventions here while testing).
.vscode/settings.json
*.tsbuildinfo
test-results/
playwright-report/
releases/*.vsix
packages/app-extension/examples/sanitized-template-new.html

# Local brainstorming, not part of the repo
PUBLISHING.md
