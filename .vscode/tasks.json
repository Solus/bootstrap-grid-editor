{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build extension",
      "detail": "One-shot build of the webview + host bundles (used by the Run config).",
      "type": "shell",
      "command": "npm run build -w bootstrap-visualizer-extension",
      "group": "build",
      "problemMatcher": []
    },
    {
      "label": "watch webview",
      "detail": "Rebuild the webview bundle on change (run manually; then Reload the webview).",
      "type": "shell",
      "command": "npm run watch:webview -w bootstrap-visualizer-extension",
      "isBackground": true,
      "problemMatcher": [],
      "presentation": { "group": "watch", "panel": "dedicated" }
    },
    {
      "label": "watch host",
      "detail": "Rebuild the host bundle on change (run manually; then Reload Window in the dev host).",
      "type": "shell",
      "command": "npm run watch:host -w bootstrap-visualizer-extension",
      "isBackground": true,
      "problemMatcher": [],
      "presentation": { "group": "watch", "panel": "dedicated" }
    }
  ]
}
